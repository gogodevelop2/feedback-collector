function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);
  
  sheet.appendRow([
    new Date(), // 타임스탬프
    data.feedback // 피드백 내용
  ]);
  
  return ContentService.createTextOutput("Success");
}
